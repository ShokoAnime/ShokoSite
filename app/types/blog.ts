export type PostCardProps = {
  file: string;
  image: string;
  title: string;
  date: string;
  description: string;
};

export type PostContributorsProps = {
  devs: string[];
};

export type PostTagsProps = {
  tags: string[];
};
